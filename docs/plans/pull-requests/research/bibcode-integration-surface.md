# Git Manager integration map (every wiring point a sibling module must mirror)

Captured 2026-09-20 from the working tree at 15fabc78. Line numbers drift.

## 1. Left sidebar project item

- `apps/web/src/components/Sidebar.tsx:3222-3237` — Git Manager chip: `Tooltip`/`TooltipTrigger` wrapping `<button type="button">`, `aria-label={`Git Manager for ${project.displayName}`}`, `data-testid="git-manager-button"`, `className={SIDEBAR_ICON_ACTION_BUTTON_CLASS}`, icon `GitBranchIcon`, tooltip "Git Manager". Siblings: `new-main-chat-button` (3190-3205), `new-worktree-button` (3206-3221).
- `Sidebar.tsx:2548-2557` — `openGitManagerForProjectMember`: closes mobile sidebar, `navigate({ to: "/project/$environmentId/$projectId/git", params })`.
- `Sidebar.tsx:2559-2564` — `handleOpenGitManagerClick` → `runProjectMemberAction(event, openGitManagerForProjectMember)` (grouped-member dispatch shared by all per-project actions).
- `Sidebar.tsx:3105-3107` — `aria-current={gitManagerActive ? "page" : undefined}`, `isActive={gitManagerActive || projectSelected}` on the project header row.
- `Sidebar.tsx:1509,1532` — `gitManagerActive: boolean` prop on the project-row component; call sites `:4019`, `:4054` pass `gitManagerActive={gitManagerRouteProjectKey === project.projectKey}`.
- `Sidebar.tsx:4410-4419` — `gitManagerRouteProjectKey` memo: `pathname.endsWith("/git")` → route project key via `projectPhysicalKeyByScopedRef` → `physicalToLogicalKey`; threaded at `:3812,3864,4889`.
- No command palette or keybinding entries.
- Tests: `apps/web/src/components/Sidebar.test.tsx:3633` (aria-label), `:3658` (highlight on route), `:3680-3683` (idempotent navigation), `:3742-3750` (grouped member navigation).

## 2. Routing

- `apps/web/src/routes/_chat.project.$environmentId.$projectId.git.tsx` (46 lines): `createFileRoute("/_chat/project/$environmentId/$projectId/git")({ component: GitManagerRouteView })` (:43-45). `GitManagerRouteView` (:14-24): `Route.useParams()`, `scopeProjectRef(environmentId, projectId)` (`@bibcode/client-runtime/environment`), `useProject(projectRef)`, `useEnvironmentQuery(environmentShell.stateAtom(...))`; redirect to `/` after `MISSING_PROJECT_REDIRECT_DELAY_MS` (1000 ms) when the project is missing and the shell is live (:27-33); renders `<ChatRouteInset><GitManagerPanel projectRef={projectRef} /></ChatRouteInset>` (:36-40).
- `apps/web/src/routeTree.gen.ts` is generated by the TanStack Router Vite plugin; never hand-edit.

## 3. Web state and RPC glue

- `apps/web/src/gitManagerStore.ts` (445 lines): zustand + persist, `GIT_MANAGER_STORAGE_KEY = "bibcode:git-manager-state:v1"`, `GIT_MANAGER_STORAGE_VERSION = 1`, `GIT_MANAGER_VIEW_STATE_LIMIT = 2`, `byProjectKey`/`toolbarByProjectKey` keyed by `projectKey(ref)`.
- `apps/web/src/state/gitManager.ts` (92 lines): `gitManagerEnvironment = createGitManagerEnvironmentAtoms(connectionAtomRuntime)`; `runGitManagerOperation(registry, target, onEvent)` mounts the `gitManager.runOperation` stream via `runStreamInEnvironment`.
- `packages/client-runtime/src/state/gitManager.ts` (100 lines): `createGitManagerEnvironmentAtoms(runtime)` — read queries via `createEnvironmentRpcQueryAtomFamily` (`getRefs`, `getCommits`, `getDiff`, `getStashes`, `getRemoteTags`, `previewMerge`, `listPullRequests`; each with `label`, `tag: WS_METHODS.*`, `staleTimeMs`); `signal` via `createEnvironmentRpcSubscriptionAtomFamily` (`idleTtlMs: 0`); mutations via `createEnvironmentRpcCommand` sharing `vcsCommandScheduler`/`vcsCommandConcurrency`; `runOperation` via `createEnvironmentRpcStreamCommand`. Factories live in `packages/client-runtime/src/state/runtime.ts:592,613,645,665`.
- `packages/client-runtime/package.json:66-68` — subpath export `"./state/git-manager"`; a sibling needs its own `"./state/pull-requests"` entry.
- `packages/client-runtime/src/state/git.ts` + `apps/web/src/state/git.ts` — pre-existing `git.*` workflow methods (`git.runStackedAction`, `git.resolvePullRequest`, `git.preparePullRequestThread`) used by `GitActionsControl` and `resolveThreadPr` (`Sidebar.tsx:720`).
- `apps/web/src/components/gitManager/gitManagerAvailability.ts` (105 lines): `resolveGitManagerAvailability` gated on `capabilities.gitManagerReads` (:100-103); `resolveGitManagerCapabilityDisabledReasons` (:32-57).
- `apps/web/src/components/gitManager/GitManagerPanel.tsx` — panel shell composing tabs (`Tabs`/`TabsList`/`TabsPanel`/`TabsTab`), `resolveGitManagerDefaultTab`.

## 4. Contracts

- `packages/contracts/src/gitManager.ts` (499 lines): blocked reasons (:7-25), refs (:40-74), commits (:76-101), diff (:103-145), changed files (:147-164), stash/conflict/merge preview (:166-199), `GitManagerOperationRequest`/`GitManagerOperationEvent` (:218-350), signal (:352-356), per-RPC inputs/results incl. `GitManagerPullRequestEntry`, `GitManagerCheckEntry`, `GitManagerPullRequestsResult` (:358-489), `GitManagerOperationError` (:491).
- `packages/contracts/src/rpc.ts`: `WS_METHODS` Git Manager block (:399-413); `Rpc.make(...)` per method (:1095-1180), naming `Ws<PascalMethod>Rpc`, `WsGitManagerRunOperationRpc` has `stream: true`; `WsSubscribeGitManagerSignalRpc`; `WsRpcGroup = RpcGroup.make(...)` (:1497,1563-1576) — every new Rpc must be added here (client dispatcher + parity test).
- `packages/contracts/src/index.ts:22` — `export * from "./gitManager.ts"`.
- `packages/contracts/src/environment.ts:30-53` — `ExecutionEnvironmentCapabilities` flags (`gitManagerReads`, `gitManagerCommitOperations`, `gitManagerBranchSyncOperations`, `gitManagerStashMergeOperations`, `gitManagerPartialStaging`, `gitManagerRewriteOperations`, `gitManagerTagOperations`, `gitManagerLiveSignal`, `gitManagerPullRequests`), each `withDecodingDefault(false)`.
- Fixtures: `packages/contracts/fixtures/rpc-wire/stream-shapes/gitManager__runOperation-0{0..3}.json`, `typed-failures/gitManager__*.json`; verified by `packages/contracts/src/rpcRustParity.test.ts:296-` against `fixtures/rpc-wire/manifest.json`. Adding an Rpc requires matching fixtures.

## 5. Server

- `apps/server/src/git/manager/`: `mod.rs` (+ `#[cfg(test)] mod telemetry`), `conflicts.rs`, `generation.rs`, `graph.rs`, `guards.rs`, `in_progress.rs`, `merge.rs`, `operations.rs`, `patch.rs`, `refs.rs`, `rewrite.rs`, `stash.rs`, `tags.rs`.
- `apps/server/src/production/git_manager_rpc.rs` (2332 lines): `GIT_MANAGER_UNARY_METHODS` (13) and `GIT_MANAGER_STREAM_METHODS` (:54-71); `GitManagerRpcServices`/`ConfiguredGitManagerRpcServices` (holds `pull_requests: PullRequestService` :117), `.with_pull_request_service()` (:143); `with_dependencies(...)` (:89-108); `register_git_manager_rpc(registry, services)` (:968-1034) registers stubs for unknown names, 7 reads → `handle_read_unary`, 6 mutations → `handle_mutation_unary`, 2 streams; `list_pull_requests` (:876-947); registration tripwire test `registers_every_git_manager_method_needed_by_production_startup` (~:1866).
- `apps/server/src/production/runtime.rs:38-40,423-434` — constructs services and calls `register_git_manager_rpc`; a sibling registers right after.
- Provider layer: `apps/server/src/source_control/{mod.rs, pull_request.rs, checks.rs, discovery.rs}` (see server-provider-layer.md).
- Scopes: `apps/server/src/auth/scope.rs:11 required_scope`; reads :21-64, operate :65-74; test arrays :217-251.
- Capabilities populated: `apps/server/src/production/control.rs:2152-2166` (`server.getConfig` literal) and `:5044-5054` (second enumeration; verify purpose).
- Tripwires: `git/manager/mod.rs:109-110 every_git_manager_read_and_operation_is_non_interactive_git`, `:471-472 explicit_checks_handler_is_the_only_non_git_process_surface`.

## 6. Docs

- `docs/user/workspace-ui.md:264- ## Git Manager`, `:86` sidebar highlight, `:298,367,475,563-564`.
- `docs/user/keybindings.md:107-110` — no Git Manager keybinding commands.
- `docs/architecture/overview.md:182,718,826,829`.
- `docs/architecture/rpc-and-orchestration.md:214-220,233- ## Git Manager flow` (method→scope table :240-243), `:245-261`, `:1172-1177`.
- `docs/integrations/source-control-providers.md:15,71-89,192`.
- Plan folder format `docs/plans/git-manager/`: spec (§1 Outcome … §12 Decision log), plan (Global Constraints, Why this shape, Architecture{Ownership,Server,Contracts,Client}, Slices, Risks, Validation, Documentation to update), `phases/PHASE-NN-<slug>.md` (Files, Dependencies, Owner Agent, Risk / Effort, Skills to Invoke, Documents to Read, Pre-execution check, Atomic steps, Verification, Notes for downstream phases), `tasks.md` (status table `# | Phase | Round | Owner agent | Status | Agent | Started | Finished`), `execute-plan.md` (rounds + coordinator prompt), `handoff.md`, `research/`.

## 7. Tests

- Sidebar: `Sidebar.test.tsx:3633,3658,3680-3683,3742-3750`.
- Panel: `GitManagerPanel.test.tsx`, `GitManagerPanel.lifecycle.test.tsx`, `gitManagerTelemetry.test.tsx` (:251 zero-telemetry runtime: no request/Image on render, no provider access on idle, exactly one provider command on explicit Refresh).
- Sub-view tests co-located per slice (provider/`GitManagerPullRequestPanel.test.tsx` + `.logic.test.ts`, provider/`GitManagerCreatePullRequestDialog.test.tsx`, …).
- Server: `apps/server/tests/git_rpc.rs`, `git_manager_reads.rs`, `git_manager_commit.rs`, `production_git_manager_rpc.rs` (2291 lines), `production_git_vcs_rpc.rs`, `git_coverage.rs`, `linux_appimage_git_environment.rs`.
- Contracts: `gitManager.test.ts`, `git.test.ts`, `rpcRustParity.test.ts`.
- e2e: `apps/desktop/e2e/specs/pierre-diffs.e2e.ts:394-403` clicks `button[aria-label="Git Manager for ${label}"]` and asserts URL contains `/git`.

## 8. Gating

- Capabilities declared in `environment.ts:30-53`, populated in `control.rs:2152-2166` and `:5044-5054`, consumed by `gitManagerAvailability.ts`, defaulted all-false by `packages/shared/src/testSupport.ts:4-26 makeTestExecutionEnvironmentCapabilities(overrides)`.
- Every method also needs a scope in `auth/scope.rs` (default-deny otherwise).
